# k6 test suite — gitignore

# Generated at runtime — contain secrets and DB-specific UUIDs
tokens.json
fixtures.json
results/

# Grafana + InfluxDB persistent volumes (docker compose)
grafana/data/
influxdb/data/

# Node modules (if you add a package.json here)
node_modules/
