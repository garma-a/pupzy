info:
  name: update_profile_mutation
  type: graphql
  seq: 3

graphql:
  method: POST
  url: "{{BaseUrl}}"
  body:
    query: |
      mutation updateProfile($input: UpdateProfileInput!) {
        updateProfile(input: $input) {
          id
          fullName
          city{
            id,
            nameEnglish,
            governorate,
            
          }
        }
      }
    variables: |-
      {
        "input": {
          "fullName": "patrick",
          "phoneNumber":"+201278203355"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
